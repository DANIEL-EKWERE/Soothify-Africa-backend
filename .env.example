# Copy to .env for local overrides. Nothing here is required for local dev —
# the defaults in settings.py already give you DEBUG + SQLite.

# DJANGO_DEBUG=True
# SECRET_KEY=generate-your-own-for-production
# ALLOWED_HOSTS=soothifyafrica.com,www.soothifyafrica.com
# DATABASE_URL=postgres://user:pass@host:5432/dbname
